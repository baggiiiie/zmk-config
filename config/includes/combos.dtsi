/{
   
    combos {
        compatible = "zmk,combos";
        
        combo_esc {
            timeout-ms = <50>;
            key-positions = <2 3>;
            // global-quick-tap-ms = <75>;
            bindings = <&kp ESC>;
        };

    };
};